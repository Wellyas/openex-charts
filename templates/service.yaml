apiVersion: v1
kind: Service
metadata:
  name: {{ include "openex-charts.fullname" . }}
  labels:
    {{- include "openex-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openex-charts.selectorLabels" . | nindent 4 }}
