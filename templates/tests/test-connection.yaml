apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openex-charts.fullname" . }}-test-connection"
  labels:
    {{- include "openex-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openex-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
